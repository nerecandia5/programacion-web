const array = ["Candia Nerea", "pizza", "hamburguesa"];



function procesarPedido(pedidos) {
//codigo

const clente = pedidos.shift();
const bebidas = "sprite"
pedidos.unshift(bebida)

}

consoles.log(procesadorPedido(array));

while(contador <array.length) {
    console.log(array)